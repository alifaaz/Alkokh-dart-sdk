name: alkokh_mobile_sdk
description: Pure Dart SDK for Alkokh mobile API auth and Sales Order based mobile orders.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
